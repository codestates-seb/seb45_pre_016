spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:test
  jpa:          #JPA 설정
    hibernate:
      ddl-auto: create
    show-sql: true

logging:         #로그 레벨 설정
  level:
    org:
      springframework:
        orm:
          jpa: DEBUG